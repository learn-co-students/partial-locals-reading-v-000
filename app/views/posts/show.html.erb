Information About the Post
<%= render partial: "authors/author", locals: {post_author: @author} %>
<!-- No longer passomg the render method a string, we're passing a hash with two key value pairs -->
<%= @post.title %>
<%= @post.content %>
